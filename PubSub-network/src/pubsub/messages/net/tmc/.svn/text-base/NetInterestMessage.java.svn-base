package pubsub.messages.net.tmc;

import pubsub.ByteIdentifier;
import pubsub.ForwardIdentifier;
import pubsub.messages.MessageType;

/**
 *
 * @author John Gasparis
 */
public class NetInterestMessage extends TMCNetMessage {

    public NetInterestMessage(MessageType.Type msgType, ForwardIdentifier fid, ByteIdentifier routerID) {
        super(msgType, fid, routerID);
    }
}
